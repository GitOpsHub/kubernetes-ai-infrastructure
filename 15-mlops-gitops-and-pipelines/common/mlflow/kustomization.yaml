apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: []
# This directory holds Helm values files consumed by common/argocd-apps/apps-*/app-mlflow.yaml
# (or cpu-lab/install-mlflow.sh) — no standalone Kubernetes objects of its own to kustomize.

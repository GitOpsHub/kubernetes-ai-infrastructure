# Identity for training pods. On EKS, Mountpoint for S3 uses the CSI driver's own
# IRSA identity (see the storage setup in README §4.3), so this SA needs no annotation.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: trainer
  namespace: ch07-training

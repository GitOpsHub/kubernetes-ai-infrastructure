# Deliberately named "gpu-operator" (chapter 02's real namespace) so common/servicemonitor's
# ServiceMonitor (namespaceSelector: gpu-operator, matchLabels: app=nvidia-dcgm-exporter) works
# UNCHANGED against this fake exporter - the whole point of the lab is to exercise the same
# Prometheus/Grafana/alerting wiring you'll use with a real GPU cluster.
apiVersion: v1
kind: Namespace
metadata:
  name: gpu-operator
  labels:
    course.chapter: "04"
    lab: cpu-fake-dcgm

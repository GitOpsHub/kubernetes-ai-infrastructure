# What vLLM serves. Overlays merge their own serving.env on top of this one.
# Day 1: straight from the Hub. After a pipeline run, set it to the workflow's output:
#   argo get -n ch19-pipelines <workflow> -o json | jq -r '.status.outputs.parameters[] | select(.name=="model-path").value'
MODEL_PATH=Qwen/Qwen3-0.6B

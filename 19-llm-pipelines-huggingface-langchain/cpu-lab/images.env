# Written by build-load-kind.sh: a local CPU-torch image loaded straight into the cluster's nodes
# (non-"latest" tag -> imagePullPolicy IfNotPresent, so the node never tries a registry).
TRAINER_IMAGE=ch19-trainer:cpu

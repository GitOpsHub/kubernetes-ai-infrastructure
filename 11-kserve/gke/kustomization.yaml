# GKE: run ./install-kserve.sh first (reuses the "spot-gpu" L4 pool from 01-gpu-nodes-and-scheduling
# for the generative/ LLMInferenceService — create it first if you haven't).
#   kubectl apply -k 11-kserve/gke                 # namespace + predictive (CPU, no GPU needed)
#   kubectl apply -k 11-kserve/gke/generative       # LLMInferenceService (GPU)
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- ../common
- ../common/predictive
